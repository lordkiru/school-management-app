// Single source of truth for the school levels/sections a class can belong to,
// and that a school can choose to operate (see School.schoolLevels).
const SCHOOL_LEVELS = ['Creche', 'Kindergarten', 'Nursery', 'Primary', 'Secondary'];

module.exports = { SCHOOL_LEVELS };
